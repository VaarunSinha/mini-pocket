# HTTP Basic Auth (required for /sync and /auth/login)
# Set these before running the server; only this username/password will be accepted.
BASIC_AUTH_USERNAME=your_username
BASIC_AUTH_PASSWORD=your_password

# Optional: SQLite database path (default: backend/data/pocket.db)
# DATABASE_PATH=./data/pocket.db
